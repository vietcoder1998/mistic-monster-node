export enum MonsterType {
    REPTILE = 'reptile',
    BIRD = 'bird',
    BEAST = 'beast',
}

export enum AccountType {
    BANKER = 'banker',
    USER = 'user',
}

export enum CoinUnit {
    Monster = 'MMC',
    DgCoin = 'DGC',
    Dollar = 'USD',
}

export enum TransactionType {
    Mining = 'mining',
    Transfer = 'transfer'
}