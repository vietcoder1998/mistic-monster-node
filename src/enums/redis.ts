enum StoreSymbol {
    blocks = 'blocks',
    txs = 'tsx',
    accounts = 'accounts',
    wallets = 'wallets',
    contracts = 'contracts',
    privacy = 'privacy',
    nodes = 'nodes',
}

export { StoreSymbol }
